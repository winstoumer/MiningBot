import {TonConnectButton} from "@tonconnect/ui-react";
import TelegramUser from "../TelegramUser/TelegramUser";
import './header.scss';

export const Header = () => {
    return <header>
        <TelegramUser />
    </header>
}
